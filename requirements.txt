# PyTorch must be installed separately — pick the wheel that matches your CUDA:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# (cu124 = CUDA 12.4, works on A100/H100; use cu118 for older drivers)

numpy>=1.24
scipy>=1.11
nibabel>=5.0
einops
timm
pyyaml
matplotlib
monai>=1.3
tqdm
scikit-image
huggingface_hub
